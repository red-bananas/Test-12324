export const siteConfig = {
  name: "Clone",
  description: "A fast, private, client-side tool. Works offline, no signup.",
  url: process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000",
} as const;
