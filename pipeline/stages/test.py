from __future__ import annotations


def run(clone_id: int) -> None:
    print(f"[test] not yet implemented (clone_id={clone_id})")
    print("  next: web -> vitest + playwright; mobile -> jest + expo export + emulator smoke")
