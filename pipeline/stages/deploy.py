from __future__ import annotations


def run(clone_id: int) -> None:
    print(f"[deploy] not yet implemented (clone_id={clone_id})")
    print("  next: open clone-repo PR. web -> Vercel; mobile -> eas update + eas build --profile preview")
