from __future__ import annotations


def run(candidate_id: int) -> None:
    print(f"[spec] not yet implemented (candidate_id={candidate_id})")
    print("  next: DeepSeek reads homepage HTML + screenshots -> emits clone-spec.json")
