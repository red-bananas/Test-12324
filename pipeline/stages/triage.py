from __future__ import annotations


def run(candidate_id: int | None = None) -> None:
    print(f"[triage] not yet implemented (candidate_id={candidate_id})")
    print("  next: open GitHub issue per discovered candidate with screenshot + tractability score")
