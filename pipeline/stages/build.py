from __future__ import annotations


def run(candidate_id: int) -> None:
    print(f"[build] not yet implemented (candidate_id={candidate_id})")
    print("  next: Aider subprocess loop, 6 iterations max, $1/run cap, lane-aware template")
